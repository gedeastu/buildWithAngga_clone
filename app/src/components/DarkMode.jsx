import React from 'react'
import { useThemeContext } from '../hook/StateGlobal'
const DarkMode = () => {
  const {theme,setTheme} = useThemeContext()
  return (
    <>
    
    </>
  )
}

export default DarkMode